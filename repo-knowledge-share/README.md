# repo-knowledge-share (retired)

Replaced by the `kb` skill (2026-07-15). Knowledge now lives in the Life OS wiki:
`/Users/derekxwang/Development/projects/DXW/mono/os/wiki/` (project pages + `pattern-*.md`).
Per-machine config moved from `~/.config/repo-knowledge-share.json` to `~/.config/kb.json`.
History of the old knowledge files is preserved in git.

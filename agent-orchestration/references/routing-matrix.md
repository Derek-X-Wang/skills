# Operating routing matrix

Last reviewed: 2026-09-12; operating-policy review only, model capability claims and dated preferences keep their own dates.

These are Derek's current operating preferences. They combine model capability, available subscriptions, route friction, and quota. They are not a pure intelligence ranking. The orchestrator owns model, effort, harness, route, and quota selection and verifies the resolved route, tools, checkout, and return path. Workers follow their assigned route and check only task checkout/tools/access; they do not reload this table or perform quota/host investigation.

## Current access paths

| Entitlement | Current harness | Model access | Current routed use |
| --- | --- | --- | --- |
| Codex subscription | Codex | OpenAI GPT models | Native Codex coordination for harness-owned agents; verified host route for external sessions |
| Claude Code subscription | Claude Code | Anthropic Claude models | Native Claude coordination for harness-owned agents; verified host route for external sessions |
| OpenCode Go | OpenCode | Curated multi-provider model pool centered on open-source models | Foreground `opencode run` for focused one-shot workers; attended Orca terminal as a manual fallback |

## Quota-aware worker routing

Every dispatch decision requires sufficiently fresh relevant entitlement evidence. At implementation, research, or review selection, the orchestrator reuses a genuinely fresh subscription-usage snapshot from the verified session; otherwise it refreshes before selecting the model and route. When a refresh is needed and the current machine can reach its local Orca CLI/runtime, inspect Orca's version-matched public account surface, even when another harness or host UI contains the session. Read [orca.md](runtimes/orca.md) and load `orca-cli` for that operation. Treat unavailable Orca or missing, stale, or failed provider data as unverified. An unverified branch is a bounded provisional route, not an availability claim; verify it before sustained or parallel worker volume. Do not install or invoke a second quota checker automatically, inspect credential storage, or rely on an outdated snapshot. Use another quota source only when Derek explicitly requests it.

Apply this balancing after explicit route requests, role eligibility, authorization, review independence, checkout isolation, required tools, and return-path safety:

- Reuse fresh usage at the dispatch decision. Judge freshness against reset timing, intervening consumption, concurrent activity, and expected task size. Refresh stale or changed data, after resets or rate-limit events, after real route/tool/host changes affecting the evidence, and before sustained volume. Do not blindly poll every child dispatch or send quota checking to workers. A user report that an entitlement is near or at its limit invalidates contradictory prior headroom and triggers a refresh; missing, stale, or failed data never counts as healthy.
- Compare short-window, weekly, model-specific, and monthly limits together with reset timing, expected task cost and duration, and pool reserves. For Claude, require sufficiently fresh shared Claude and model-specific pools under the reuse/refresh policy above. Percentages from different plans are not equal token budgets; API prices and costs do not prove subscription allowance consumption.
- There is no mandatory spend order, fixed equal split, or cheapest-at-any-cost rule. Low utilization on one plan does not obligate spending it first.
- Treat unknown usage as unknown. Never assume an entitlement is empty or route to it automatically. A bounded provisional route is allowed; verify before sustained volume.
- Apply Derek's role defaults among sufficient eligible options with adequate headroom. Rebalance when material capacity constraints, reset timing, or upcoming demand justify another eligible route. Reserve Fable for difficult judgment and explicitly authorized orchestrator handoffs; GPT-5.6 Sol at high effort is the normal Codex worker default. Astra is specialist-only under the role rows below; medium is available for an explicit request or a specifically identified difficult implementation scope, not routine work. Preserve Opus capacity when it is in use elsewhere. Abundant Codex remains useful for suitable coding and review.
- Role defaults are conditional on actual remaining budget and reset timing. When Codex is near or at its limit without headroom for the expected work, route new ordinary workers to a sufficient authorized Claude model — Opus, or Sonnet for implementation only — or to an OpenCode Go option with actual quota; native or default convenience is not a reason to keep spawning Codex workers. There is no fixed reserve percentage or threshold: judge headroom against the expected work and reset timing. If an explicitly requested route cannot run, report the limit rather than silently substituting.
- Track the entitlement a route actually consumes, not the model's provider: GPT-5.6 Luna through OpenCode Go consumes OpenCode Go allowance, not Codex, while the same model in Codex consumes Codex.
- Consider eligible OpenCode Go GLM-5.3-Flash workers proactively whenever spare capacity exists, including when Codex is healthy.
- Also consider the approved DeepSeek V4.1 Flash and Luna-max bounded trials below. A verified temporary promotion can favor a suitable trial over a standing preference; trial eligibility is not established production quality and never lowers acceptance or review requirements.
- Quota never makes an unsafe route eligible. Balancing may not bypass authority, required tools, checkout isolation, review independence, or the return path.

The GLM-5.3-Flash route requires an owned foreground `opencode run` for bounded work or an attended Orca terminal. If the task must run unattended or AFK, or no orchestrator can attend the terminal, skip this route and choose the next eligible verified route.

Classify difficulty by coherent work phase or slice, not by the whole ticket; model and effort may vary by phase: retain strong judgment on orchestration, planning, and undecided or difficult work, and route decided implementation to a sufficient implementer. Prefer eligible OpenCode Go GLM-5.3-Flash for bounded implementation and for repository or supplied-material research. Skipping it needs a concrete capability, tool, quota, route, or coupling/overhead reason; native convenience alone is insufficient.

Separate web search and source discovery from repository and supplied-material research. For web work, prefer Codex or Claude sessions with verified hosted search tools — a dated user preference, not a provider-superiority claim. OpenCode remains eligible for repository investigation and supplied documents. Tools do not inherit across routes: a route without a verified required search tool stays unavailable for that web work. The foreground and AFK restrictions above still apply.

Ignore banked Codex rate-limit reset credits during routing and never claim they are already-available current quota. Derek redeems a reset only by explicit request. After Derek uses one, refresh the usage data and route from the new utilization instead of the prior value or the remaining reset-credit count.

### Temporary DeepSeek V4.1 Flash promotion

Observed 2026-09-10: the [OpenCode Go page](https://opencode.ai/go) advertises 4× usage for DeepSeek V4.1 Flash for a limited time. Derek reports a 72-hour window; the inspected public page did not establish an exact start/end timestamp or timezone. Treat the duration as user-reported, not a verified rolling window starting whenever this skill is loaded. Before counting bonus capacity, revalidate current official terms, the absolute cutoff and account applicability, alongside fresh actual remaining quota. If expired or the cutoff/applicability cannot be verified, ignore the bonus in capacity planning and use verified ordinary limits or mark capacity unknown. Never multiply every Go model's allowance, double-count an already-adjusted balance, add work merely to spend the promotion, or bypass task fit, consent, authority, review, budgets, or foreground-route requirements. This note does not schedule work, purchase credits, or change permanent defaults.

## Role defaults

For decided, bounded work, the explicit candidate row permits bounded trials rather than a default promotion; there is no blanket small-only gate, but a trial never gains automatic eligibility for hard or high-risk judgment or reviewer roles. Choosing a candidate for its task fit and verified quota opportunity is a concrete reason to depart from GLM for that trial; it is not native convenience alone.

| Role | Preferred target | Practical alternative | Operating reason |
| --- | --- | --- | --- |
| New orchestration | GPT-5.6 Sol at high effort in Codex; Opus in Claude Code | The explicitly user-selected capable orchestrator | Dated preference, 2026-09-12: neither Astra nor Fable is an automatic orchestration default or quota fallback. Keep explicit user choices and the active orchestrator intact. |
| Difficult orchestration escalation | Keep the active orchestrator (normally Sol or Opus); request bounded Astra/Fable judgment when concretely needed | Ask Derek to authorize a full handoff to Astra or Fable | Prefer a focused adviser over transferring ownership. Apply specialist review/judgment criteria, quota and required tools. Difficulty alone does not authorize replacing the orchestrator; the root skill owns explicit approval and capability-loss handoffs. |
| Web search and source discovery | Codex GPT-5.6 Sol at high effort or Opus in Claude Code, in a session with verified hosted search tools | GLM-5.3-Flash where its route verifies hosted search tools; another justified research-capable worker with verified hosted search tools | Dated operating preference, 2026-09-08: web work prefers Codex or Claude sessions with verified hosted search tools; tools do not inherit across routes and missing tools stay missing. Preference only, not fresh capability-test evidence. Ordinary research is not Fable work by default. |
| Repository and supplied-material research | Opus, GLM-5.3-Flash, or Codex GPT-5.6 Sol at high effort by evidence, tools, and quota, with eligible OpenCode GLM preferred per the phase rules above | Another justified research-capable worker | Sol high is the default when normal research is routed to Codex. Ordinary research is not Fable work by default. |
| Implementation | GLM-5.3-Flash through OpenCode Go for ordinary bounded implementation when sufficient, authorized, and route/quota eligible | Codex GPT-5.6 Sol (`gpt-5.6-sol`) at high effort — the default within Codex; Opus in Claude Code when delegated routes are constrained; Astra only for an explicit request or a specifically identified difficult implementation scope | GLM remains Derek's preferred capability, price, and allowance balance. Derek restored Sol high on 2026-09-08 after reporting that Astra medium consumed weekly Codex allowance too quickly; this supersedes the 2026-09-06 Astra-medium worker default, not a measured savings claim. State the concrete unresolved problem, bounded scope, evidence, and stop contract for the Astra exception; a whole-ticket complexity label or generic quality preference is insufficient. Task difficulty and required tools precede defaults and quota balance; do not stage a weak-model failure when hard reasoning is already demonstrated. Explicit task model requests always win. Quota chooses only between eligible models. |
| Bounded chore | DeepSeek V4 Flash through a foreground `opencode run` for small, clear, readily verifiable implementation and chore tasks, when eligible | GLM-5.3-Flash or Sonnet in Claude Code; any verified capable worker otherwise | A DeepSeek data-location consent gate was observed on 2026-08-28; verify current consent state before use. Changing that choice requires Derek's explicit approval; existing approval does not need to be requested again. Never select DeepSeek as reviewer. Follow model-profile eligibility for other roles. Preserve stronger-model capacity. |
| Bounded-worker candidates (implementation/chore trials) | Proactively consider DeepSeek V4.1 Flash (`opencode-go/deepseek-v4.1-flash` as observed later on 2026-09-10; an earlier same-day catalog read showed `opencode-go/deepseek-flash`, so verify the resolved ID at dispatch) with task-selected supported effort, or GPT-5.6 Luna at explicit max (`gpt-5.6-luna` in Codex; `opencode-go/gpt-5.6-luna` in Go) | Existing sufficient GLM, Sol-high, or Sonnet worker under their role rules | Follow [candidate eligibility](model-profiles.md#bounded-worker-candidates-2026-09-10): decision-complete, readily verifiable bounded work; actual model/effort, tools, consent, entitlement and return route verified. Local capability remains untested. Neither candidate is reviewer-eligible. OpenCode requires owned foreground work or an attended terminal, never an assumed AFK route. The DeepSeek promotion influences only suitable trials while verified active. |
| Complex computer use | A capable session that has loaded `computer-use-routing` | Any eligible local adapter the router approves | Model ability does not prove an adapter exists; `computer-use-routing` owns adapter eligibility and may return `executor_required`. |
| OpenCode implementation worker | GLM-5.3-Flash through a foreground `opencode run` for one-shot work | GLM-5.3-Flash through an attended Orca terminal; DeepSeek V4 Flash through a foreground `opencode run` for bounded one-shot work with current data-location consent verified | Foreground route rules for OpenCode Go workers: do not detach the CLI or treat the current Orca route as AFK-capable. Verify the exact resolved model before dispatch. Never silently substitute GLM-5.3 for GLM-5.3-Flash or inherit its evidence. |
| Review of Claude work | GPT-5.6 Sol through a verified Codex route, Opus, or GLM-5.3-Flash through a verified OpenCode route, chosen by quota balancing | Another existing reviewer-eligible model, including a different reviewer-eligible Claude model or fresh Astra high for important work | Preserve existing sufficient reviewer options. Astra can satisfy cross-model review when the actual implementer is another model. Follow [review-policy.md](review-policy.md) for sufficiency, independence, and Fable escalation. |
| R2 review of Astra-authored work — applies before generic GPT choices | Claude Opus 5 at max effort through a verified Claude route, when sufficient and route/quota eligible | Another sufficient reviewer-eligible different model; fresh Astra high can add useful same-model review but cannot satisfy this gate | Dated user preference, 2026-09-06. Use a fresh read-only independent session able to assess critical risks. Sol high is the default Codex implementer, not the default primary reviewer of important Astra work. Follow [review-policy.md](review-policy.md); unavailable Opus or lower quota alone never triggers Fable. |
| Review of GPT work | Opus through a verified Claude route, GPT-5.6 Sol through a verified Codex route, or GLM-5.3-Flash through a verified OpenCode route, chosen by quota balancing | A fresh independent session on another existing reviewer-eligible model, including Astra high for important work authored by another model | Apply the specialized Astra R2 row first. Preserve existing sufficient reviewer options for other authorships. Independence follows actual models, not provider or effort; [review-policy.md](review-policy.md) owns sufficiency, escalation, and unavailable-route blocking. |
| Reviewer or judge escalation | Fable only for a concrete unresolved critical correctness question, conflicting evidence, or demonstrated need for stronger judgment; GPT-6 Astra when useful for difficult review | The strongest sufficient reviewer-eligible model that keeps required independence | Follow [review-policy.md](review-policy.md). R2 classification or Astra authorship alone does not trigger Fable. Astra review defaults to high; targeted xhigh is reserved for unresolved critical questions or conflicting evidence. Same-model review of Astra work remains optional; cross-model review requires a different actual implementer model. |
| Stalled-worker takeover | Codex GPT-6 Astra at high or xhigh effort, selected conditionally through a verified route | Fable, only when Derek explicitly authorizes it for that takeover | Default replacement worker for a no-progress loop per [worker-takeover.md](worker-takeover.md). Choose high for fresh-context or bad-assumption recovery, and xhigh directly for demonstrated hard reasoning; no automatic high-to-xhigh retry ladder. Preserve Fable's tight allowance: never downgrade the model or effort silently, never redeem reset credits automatically, and report bounded inability when Astra is unavailable instead of spending Fable. |
| Bounded repo audit | One Codex GPT-6 Astra at high effort by default through a verified route, per [repo-audit.md](patterns/repo-audit.md) | No default substitute | Derek's selected role and effort preference for this workflow, with targeted xhigh only for a specifically identified difficult finding or question, not a second whole-repo pass. This is not a benchmark claim or a routine research default. The audit stays report-only by default, the active orchestrator is preserved, and no fanout is added. If the requested route is unavailable, report the limitation and request direction: no silent model or effort change and no automatic Fable substitution. |
| R2 review | A different model from the implementer | Same-provider different model | Prefer a different provider, but do not require it. |
| Frontend or image specialist | Verify the current Antigravity route and model | Best available proven specialist | Use only when current tools prove the required capability. |

Not defaults: Kimi and DeepSeek Pro. Reserve Kimi for justified specialist needs after verifying the exact version, effort, task evidence, and the actual entitlement pool; do not invent per-model quota and do not grant reviewer eligibility automatically. Do not route DeepSeek Pro routinely. Luna max and DeepSeek V4.1 Flash are proactively considered bounded-trial candidates under the row above, not operationally proven defaults. Older DeepSeek route names must resolve to their actual current version; do not treat a remapped V4 alias as the previously tested model or as a silent V4.1 selection.

## Review routes

Choose by review shape, not a fixed product order.

| Starting harness and need | Preferred route | Notes |
| --- | --- | --- |
| Claude Code; local Git diff review | Installed Codex plugin review command | Use its normal or adversarial review path. Verify the resolved Codex model. |
| Claude Code; custom one-shot critique | `counsel` to Codex | Use a fresh, focused prompt and inspect the returned artifact. |
| Codex; custom one-shot critique | Use the deep `counsel` profile after its target-verification step | The deep profile selected Opus when last verified; verify actual Opus 5 and max effort for the Astra R2 role, not just the profile name. Do not use a profile that resolves to Sonnet for review. |
| Either harness; focused GLM-5.3-Flash review | Foreground `opencode run` | Verify the exact executable, model, checkout when repository access is needed, independence, process ownership, structured result, and return contract. Use an isolated directory when the reviewer only needs a supplied review packet. |
| Either harness; R1 fallback | Fresh native independent subagent on a reviewer-eligible model | The same model is allowed for R1 only when its model profile permits review. Keep the reviewer read-only. |
| Either harness; visible, interactive, multi-round, or AFK review | Orca-managed external session | Use a verified terminal, checkout, mailbox, and return path. For OpenCode, the current Orca route is an attended manual fallback, not an AFK route. |
| T3 top-level provider thread control | Only a route exposed and verified by the current tool inventory | Browser preview tools do not provide agent routing. A provider CLI launched by the hosted harness is a separate route. |

Use a foreground `opencode run` for focused one-shot OpenCode work. Use Orca as an attended manual OpenCode fallback until a version-matched route proves prompt delivery and completion reporting. Do not pay its setup cost when a proven lighter route satisfies the return contract.

Preserve scarce Fable capacity for difficult judgment and explicitly authorized orchestrator handoffs. Use the simplest proven route that fits the work shape. The Sol-high worker and Opus-max Astra R2 defaults are dated user preferences, not measured quality or savings claims; max does not apply to all Opus work. Verify actual model/effort support and the reported selection at dispatch. If unavailable or unsupported, report the limit: no silent downshift, user-setting change, or automatic Fable spend. Match other effort choices to task uncertainty, complexity, and evidence. Scope, verification requirements, budgets, and stop conditions still apply. Reviewer eligibility and sufficiency override cheap or default implementer choices. Use the smallest capable model for passive watchers and bounded observation roles.
